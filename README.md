# ros2-telemetry-system
Demonstrates a multinodal architecture that publishes live streaming metrics to a subscriber node that then makes data available to an API, and ultimately to an Angular Front End.
